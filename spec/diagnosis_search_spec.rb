require 'spec_helper'

describe DiagnosisSearch do
  it 'has a version number' do
    expect(DiagnosisSearch::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
